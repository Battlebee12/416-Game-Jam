using UnityEngine;

public class testlaunch : MonoBehaviour
{
    public GameObject projectilePrefab;

    void Update()
    {
        // if (Input.GetButtonDown("Fire1"))
        // {
        //     // Example usage: Spawning a projectile with custom direction and speed
        //     Vector2 shootDirection = transform.right;
        //     BallController.SpawnProjectile(projectilePrefab, transform.position, shootDirection, 20f);
        // }

        // if (Input.GetKeyDown(KeyCode.R)) 
        // {
        //     // Example usage: Spawning a projectile at (0,0) in a random direction
        //     BallController.SpawnAtOrigin(projectilePrefab, 15f);
        // }
    }
}
